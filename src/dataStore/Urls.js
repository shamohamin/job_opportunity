
const protocol = "http" ;
const port = "3500" ;
const host = "localhost" ;
export const URLS = {
    auth : `${protocol}://${host}:${port}/api/auth`,
    advertise : `${protocol}://${host}:${port}/api/user`
};